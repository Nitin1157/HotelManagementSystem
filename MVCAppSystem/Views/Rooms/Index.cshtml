@model IEnumerable<MVCAppSystem.Models.Room>

@{
    ViewData["Title"] = "Index";
}


<div class=""> 
    <h1 id="h2">Created Rooms</h1>
    <div id="a-tag">
        <a asp-controller="Rooms" asp-action="Create" class="btn btn-primary m-2">Create Room</a>
        <a asp-controller="Admin" asp-action="Index" class="btn btn-warning m-2">Back To Dashboard</a>
    </div>
    <br />
</div>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<br />
<table class="styled-table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RoomNo)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.RoomRent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoomType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bed)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsAllocated)
            </th>*@
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoomNo)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.RoomRent)
            </td>*@
            @*<td>
                @Html.DisplayFor(modelItem => item.RoomType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bed)
            </td>*@
            @*<td>
                @Html.DisplayFor(modelItem => item.IsAllocated)
            </td>*@
            <td>
                <a asp-action="Edit" asp-route-id="@item.RoomNo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RoomNo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RoomNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
